## 1. Setup & router mount

- [ ] 1.1 Add dependency: `pnpm add react-router-dom` (v6). Confirm it appears under `dependencies` in `package.json`.
- [ ] 1.2 In `src/main.jsx`, import `{ BrowserRouter }` from `react-router-dom` and wrap `<App />` with `<BrowserRouter>` INSIDE the existing provider tree (keep order: ErrorBoundary > QueryClientProvider > AuthProvider > LanguageProvider > NotificationProvider > **BrowserRouter** > App). Do not move other providers.
- [ ] 1.3 Verify dev server boots (`pnpm dev`) and the current login screen still renders at `/` before any further changes (router mounted but routes not yet wired — App still renders old machine). This is a smoke checkpoint only.

## 2. Route skeleton, guards & compatibility shim

- [ ] 2.1 Create `src/app/RequireAuth.jsx`: a wrapper component `RequireAuth({ role, children })` that reads `useAuth()`. If `status === 'loading'` render the shared `LoadingScreen`; if anonymous render `<Navigate to="/login" replace />`; if `role==='owner'` but session is staff render `<Navigate to="/staff" replace />`; if `role==='staff'` but session is owner render `<Navigate to="/dashboard" replace />`; else render `children` (or `<Outlet/>`). Derive owner vs staff exactly as `applySessionToView` does today (`flag === '!personal' || role === 'personal' || role === 'staff'` ⇒ staff).
- [ ] 2.2 Extract `LoadingScreen` (currently inline in `src/app/AppRouter.jsx:50-59`) into `src/app/LoadingScreen.jsx` and export it; import it where used.
- [ ] 2.3 Create `src/app/RootRedirect.jsx` for path `/`: reads `useAuth()`; while `status==='loading'` render `LoadingScreen`; anonymous → `<Navigate to="/login" replace/>`; owner → `/dashboard`; staff → `/staff`. Owner-needs-onboarding handling stays in the dashboard guard (task 5.x), NOT here.
- [ ] 2.4 In `RootRedirect` (or a small `useLegacyDeepLinkRedirect` hook it calls), port the legacy-query compatibility shim from `App.jsx:62-118`, translating to navigation: `?action=verify-email&token&email` → call `apiAuthAdapter.verifyEmail(...)` then `navigate('/login', { replace:true })` and strip params; `?action=reset-password` → `navigate('/reset-password'+search, { replace:true })`; `?flow=staff-invite&biz=` → navigate to the invite portal route carrying `biz` via `location.state`. Run this BEFORE the auth landing redirect so links win.
- [ ] 2.5 Rewrite `src/app/AppRouter.jsx` into a declarative `<Routes>` tree wrapped in one `<Suspense fallback={<LoadingScreen/>}>`. Keep the existing `React.lazy` imports (SetupWizard, Dashboard, CustomerFlow, RegisterWizard, StaffRegistrationWizard, StaffDashboard, ForgotPassword, ResetPassword). Declare public + auth routes per the route tree in design.md. Do NOT yet add dashboard/staff child routes (tasks 5 & 6).
- [ ] 2.6 Declare top-level routes: `/` → `RootRedirect`; `/login` → `LoginScreen`; `/register` → `RegisterWizard`; `/forgot-password` → `ForgotPassword`; `/reset-password` → `ResetPassword`; `/onboarding` → `<RequireAuth role="owner"><SetupWizard/></RequireAuth>`; catch-all `*` → `<Navigate to="/" replace/>`.
- [ ] 2.7 Declare public deep-link routes ORDERED BEFORE the `/staff` dashboard area: `/touch/:businessSlug/:touchPointSlug` → `CustomerFlow`; `/invite/:token` → `StaffRegistrationWizard`; legacy `/staff/invite/:token` → `StaffRegistrationWizard` (sibling, declared before `/staff/*`). These render regardless of auth.
- [ ] 2.8 Reduce `src/App.jsx` to: keep top-level providers/effects that are still needed (e.g. `initStorage()` on mount, `KybGateProvider` from task 4), render `<AppRoutes/>` (the `<Routes>` now living in `AppRouter.jsx`). Remove the `view`/`setView` state, the prop-bag passed to `AppRouter`, the mount deep-link `useEffect` (moved to 2.4), the session-restore `useEffect` (replaced by guards), and the navigation handlers (`applySessionToView`, `handleLogout`, `handleResetApp`, `handleKybSuccess`, `handleKybRequired`, `triggerSimulation`, `handleQuickDemoLogin`, `handleWizardComplete`, `handleRegisterAndLogin`) — relocating their needed logic per tasks 3–7. Keep `LoginScreen` rendered via the route, not conditionally in App.
- [ ] 2.9 Smoke check: `/login`, `/register`, `/forgot-password`, `/touch/x/y`, `/invite/tok` each render the right screen; `/` redirects per auth; build passes.

## 3. Auth-flow screens → router navigation

- [ ] 3.1 `src/app/LoginScreen.jsx`: move login form state (email, password, loginError, isLoading) from App into LoginScreen local `useState`. Implement submit using `useAuth().login(credentials)`; on success compute landing (owner→`/dashboard`, staff→`/staff`, owner-needs-onboarding→`/onboarding`) and `navigate(...)`; on error set `loginError` via the existing `getErrorI18nKey` mapping. Keep the 800ms delay only if it was intentional UX (otherwise drop).
- [ ] 3.2 `LoginScreen.jsx`: replace `setView('forgot-password')` (line ~118) with `navigate('/forgot-password')`; replace the Register trigger with `navigate('/register')`. Use `useNavigate()`.
- [ ] 3.3 `src/components/ForgotPassword.jsx`: replace both `setView('login')` calls (lines ~60, ~103) with `navigate('/login')`. Remove the `setView` prop; use `useNavigate()`.
- [ ] 3.4 `src/components/ResetPassword.jsx`: replace `setView('login')` (lines ~61 success-delay, ~191 back button) with `navigate('/login')`. Keep reading `?token`/`?email` from `window.location.search` (or switch to `useSearchParams`) — behavior unchanged. Remove the `setView` prop.
- [ ] 3.5 `src/components/RegisterWizard.jsx`: replace `onBackToLogin`/`onRegisterSuccess`/`onRegisterAndLogin` prop-driven `setView` with `useNavigate()`. On register-and-login, perform the existing session fetch then `navigate('/onboarding', { state: { ssoPrefillData, isNewRegistration: true } })`; on plain success → `navigate('/login')`; on back → `navigate(-1)` or `/login`. Preserve the SSO-prefill object shape currently built in `App.handleRegisterAndLogin` (move it here or into a small helper).
- [ ] 3.6 `src/components/SetupWizard.jsx` (onboarding): read `ssoPrefillData`/`isNewRegistration` from `useLocation().state` (fallback to session/`useMerchantSetup` when absent, e.g. on refresh). On `onComplete`, `navigate('/dashboard')`. On `onBackToLogin`, call `useAuth().logout()` then `navigate('/login')`.
- [ ] 3.7 Smoke check: login as owner→`/dashboard`; login as staff→`/staff`; forgot/reset back-links land on `/login`; register→onboarding handoff carries prefill.

## 4. KYB-required gate context

- [ ] 4.1 Create `src/contexts/KybGateContext.jsx`: provider exposing `{ requireKyb(), isOpen, dismiss() }`. Render the KYB-required modal markup (port from `AppRouter.jsx:200-247`, keep i18n strings). "Verify Now" → `navigate('/dashboard/settings/kyb')` + `dismiss()`; "Cancel" → `dismiss()`. Use `useNavigate()` inside the provider.
- [ ] 4.2 Wrap the routes with `<KybGateProvider>` in `App.jsx` (above `<AppRoutes/>`), so both onboarding and dashboard can call `useKybGate()`.
- [ ] 4.3 Replace `onKybRequired` usage: `SetupWizard` and `Dashboard` call `useKybGate().requireKyb()` instead of the old prop. Remove `preKybView`, `showKybModal`, `setShowKybModal`, `initialDashboardMenu`, `initialSettingsTab` from App.
- [ ] 4.4 Decide on the in-`Dashboard` `showKybWarningModal` (`Dashboard.jsx:754-791`): minimum — its "Verify Now" navigates to `/dashboard/settings/kyb`. Optional — consolidate into `KybGateContext` (note in PR if deferred).

## 5. Owner dashboard → nested routes

- [ ] 5.1 In `AppRouter.jsx`, add the `/dashboard` route guarded by `<RequireAuth role="owner">` PLUS an onboarding gate (owner who hasn't completed onboarding → `<Navigate to="/onboarding" replace/>`; derive "completed" the same way `applySessionToView` did via session/`useMerchantSetup`). Element = lazy `Dashboard` (now a layout). Add child routes: index→`OverviewRoute`, `staff`→`StaffRoute`, `staff/:staffId`→`StaffDetailRoute`, `tips`→`TipsRoute`, `reviews`→`ReviewsRoute`, `reports`→`ReportsRoute`, `touchpoints`→`TouchpointsRoute`, `analytics`→`AnalyticsRoute`, `settings`→`SettingsRoute`, `settings/:tab`→`SettingsRoute`, `subscriptions`→`SubscriptionsRoute` (renders `ComingSoon`), `support`→`SupportRoute`, `*`→`ComingSoon` fallback.
- [ ] 5.2 Refactor `src/components/Dashboard.jsx` into a LAYOUT: keep all data hooks, `metrics`/handlers, `DashboardSidebar`, `DashboardHeader`, `MobileMenuDrawer`, and ALL modals (StaffModal x2, QrModal, AddTouchpointModal, InviteShareModal, KYB warning). Replace `{renderContent()}` in `<main>` with `<Outlet context={dashboardCtx} />` where `dashboardCtx` bundles the data/handlers each sub-view currently receives. Delete `renderContent()`.
- [ ] 5.3 Create `src/components/dashboard/routes/` with one thin component per section. Each does `const ctx = useOutletContext()` and renders the UNCHANGED sub-view with its current props: `OverviewRoute`→`<Overview/>`, `StaffRoute`→`<StaffView onViewDetail={id=>navigate('/dashboard/staff/'+id)} .../>`, `TipsRoute`, `ReviewsRoute`, `ReportsRoute`, `TouchpointsRoute`, `AnalyticsRoute`, `SettingsRoute`, `SupportRoute`, `SubscriptionsRoute`. Do not modify the sub-view components themselves.
- [ ] 5.4 Refactor `src/components/dashboard/hooks/useDashboardNavigation.js`: derive `activeMenu` from `useLocation().pathname.split('/')[2] || 'overview'`; make `navigateMenu`/`handleNavigateMenu` call `useNavigate()` (`navigateMenu` also closes mobile menu); keep `isMobileMenuOpen`, `isProfileExpanded`, `isTipsMobileExpanded`, `isTouchpointsMobileExpanded` as local state; keep the accordion `useEffect` (dep `activeMenu`); DELETE the two `initialMenu`/`initialSettingsTab` sync effects and the hook's `initialMenu`/`initialSettingsTab` params.
- [ ] 5.5 Repoint sidebar/drawer/header callbacks to `navigate`: in `DashboardSidebar.jsx` and `MobileMenuDrawer.jsx`, `setActiveMenu(id)`→`navigate('/dashboard'+(id==='overview'?'':'/'+id))`; KYB buttons (`setActiveMenu('settings')+setSettingsTab('kyb')`)→`navigate('/dashboard/settings/kyb')`; tips/touchpoints sub-tab buttons→`navigate('/dashboard/tips?tab=...')` / `'/dashboard/touchpoints?tab=...'`. `subscriptions` target→`navigate('/dashboard/subscriptions')`. In `DashboardHeader.jsx`, repoint `onViewStaffDetail`/menu callbacks similarly.
- [ ] 5.6 Staff detail via param: create `StaffDetailRoute` reading `useParams().staffId`, find the member from `ctx.staff`, render `<StaffDetailView staffMember={member} onBack={()=>navigate('/dashboard/staff')} .../>`. Remove `viewingStaffDetailId` state from the nav hook. Repoint `handleSelectLeaderboardStaff` (`Dashboard.jsx:388-394`) and any header drilldown to `navigate('/dashboard/staff/'+id)`.
- [ ] 5.7 Repoint `useStaffManagement.js`: pass `viewingStaffDetailId = useParams().staffId` and `setViewingStaffDetailId = () => navigate('/dashboard/staff')` so the delete-cleanup branch (`useStaffManagement.js:473-474`) still works without internal state.
- [ ] 5.8 Settings tab wiring: `SettingsRoute` does `const { tab='profile' } = useParams(); return <SettingsView initialTab={tab} onTabChange={t=>navigate('/dashboard/settings/'+t)} .../>`. Do NOT modify `SettingsView.jsx`/`useSettingsForm.js` (they already sync `initialTab`).
- [ ] 5.9 Tips/touchpoints sub-tab wiring: in `TipsRoute`/`TouchpointsRoute`, `const [sp,setSp]=useSearchParams(); const tab = sp.get('tab')||<default>;` pass `activeTab`/`activeSubTab` + `onTabChange={t=>setSp({tab:t},{replace:true})}`. Defaults: tips→`overview`, touchpoints→`stations`.
- [ ] 5.10 Replace "Back to Dashboard"/`ComingSoon.onBack`/overview resets with `navigate('/dashboard')`; derive the back-button visibility from URL (`activeMenu !== 'overview'`).
- [ ] 5.11 Smoke check: every sidebar item changes URL and renders; `/dashboard/settings/kyb` opens KYB tab; `/dashboard/tips?tab=savings` works; staff drilldown `/dashboard/staff/:id` + back; deleting the viewed staff returns to list; browser back/forward correct.

## 6. Staff dashboard → nested routes

- [ ] 6.1 In `AppRouter.jsx`, add `/staff` guarded by `<RequireAuth role="staff">`, element = lazy `StaffDashboard` (layout). Children from the `SCREENS` map: index→`StaffHome`, `qr`→`StaffMyQR`, `tips`→`StaffTips`, `reviews`→`StaffReviews`, `pay`→`StaffPay`, `profile`→`StaffProfile`, `notifications`→`StaffNotifications`. Ensure this is declared AFTER the public `/staff/invite/:token` route (task 2.7).
- [ ] 6.2 Refactor `src/components/staff-dashboard/StaffDashboard.jsx`: remove `activeScreen` useState; derive `screen = useLocation().pathname.split('/')[2] || 'home'`. Keep `StaffAccountProvider`, sidebar/header/bottom-nav. Pass `activeScreen={screen}` (for highlight) and `onNavigate={(s)=>navigate(s==='home'?'/staff':'/staff/'+s)}`. Replace `<ActiveView .../>` in `<main>` with `<Outlet context={{ onNavigate, onLogout }} />`.
- [ ] 6.3 Each staff screen reads `useOutletContext()` for `onNavigate`/`onLogout` (props otherwise unchanged). Read `staffId` from `useAuth().session.staffId` (via `StaffAccountProvider`) instead of the old `loggedInStaffId` prop.
- [ ] 6.4 Smoke check: `/staff` home, `/staff/qr`, `/staff/pay`, etc. render via nav and via direct URL; bottom nav highlights correctly.

## 7. Cleanup & legacy sync

- [ ] 7.1 Replace `window.history.replaceState` in `AppRouter.jsx:188` (staff-portal "return to merchant") with `navigate('/dashboard', { replace:true })`; remove the old `setView('dashboard')`/`setSetupData` plumbing (dashboard reads `useMerchantSetup` itself).
- [ ] 7.2 Confirm the `App.jsx:75` `replaceState` (verify-email param strip) is handled by the compat shim (task 2.4) using `navigate(..., {replace:true})`; remove the old code.
- [ ] 7.3 Demo tools: repoint `triggerSimulation`/`SimulationPanel` scenarios to `navigate` (`new_register`→`/register`, `staff_portal`→invite portal, `staff_dashboard`→`/staff`), keeping everything behind `isDemoToolsEnabled`. Repoint the demo "Open Setup" invite toast (`AppRouter.jsx:271-277`) and `handleQuickDemoLogin` (save setup then `navigate('/dashboard')`).
- [ ] 7.4 Grep for any remaining `setView(` and `props.view` usages across `src/` and confirm none remain (all replaced by router). Grep for stale imports of removed handlers.
- [ ] 7.5 Add a deployment note in the change/PR: production host must rewrite unknown paths to `/index.html` (SPA fallback) — e.g. Netlify `_redirects` `/* /index.html 200`. No FE code change.

## 8. Verification

- [ ] 8.1 `pnpm build` passes with no errors.
- [ ] 8.2 `pnpm test` — record failing test NAMES and compare against the ~65-failure baseline; ensure no NEW failures attributable to routing. Update/note any E2E specs that entered via `?flow=customer` or assumed the old machine.
- [ ] 8.3 Manual route walk (dev server + preview tools): owner login→`/dashboard`, all sub-menus, settings/kyb, staff drilldown; staff login→`/staff` screens; browser back/forward; refresh on `/dashboard/settings/kyb` (dev) stays on the right view.
- [ ] 8.4 Deep-link verification: `/touch/:biz/:tp`, `/invite/:token`, legacy `/staff/invite/:token`, `?flow=staff-invite&biz=`, `?action=reset-password`, `?action=verify-email&token&email` all land correctly and are not hijacked by auth redirect; `useCustomerFlow` still reads its params (API unchanged).
- [ ] 8.5 Confirm KYB gate "Verify Now" navigates to `/dashboard/settings/kyb` from both onboarding and dashboard.
- [ ] 8.6 Final report: changed files, build/test results vs baseline, and the remaining out-of-scope items (11 `?flow=customer` generators) + the SPA-fallback host note.
